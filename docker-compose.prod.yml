version: "3.8"

services:
  app:
    image: ${DOCKERHUB_IMAGE_NAME}:${GITHUB_SHA:-latest}
    container_name: ${APP_CONTAINER_NAME} # Use consistent name
    restart: unless-stopped
    env_file:
      - .env.prod

  # Service for the Nginx Reverse Proxy
  nginx:
    image: nginx:stable-alpine # Use official stable Alpine image
    container_name: nginx-proxy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/conf.d/default.conf:/etc/nginx/conf.d/default.conf:ro
      - /etc/letsencrypt:/etc/letsencrypt:ro
    depends_on:
      - app
